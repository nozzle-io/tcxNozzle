name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    uses: 2bbb/trussc-actions/.github/workflows/build-addon.yml@main
    with:
      addon_name: "tcxNozzle"
      test_app: "testApp"
      test_mode: "build-only"
      extra_apt_packages: "libdrm-dev libgbm-dev libegl-dev libgl-dev"
